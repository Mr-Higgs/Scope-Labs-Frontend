import React, { useState, useContext, useEffect } from 'react';
import api from '../api/axios';
import { VideoContext } from '../context/VideoContext';


//Display comments from videos and add comments to videos
const CommentSection = () => {
    //use state to set the comment and error

    //Send a get request to the API
    useEffect(() => {});

    //manage adding new comments to the list by post request
    const handleAddComment = acync () => {};

    //Set loader and error if content does not load
    if();

    //Display the list of comments
    return();
};

export default CommentSection;