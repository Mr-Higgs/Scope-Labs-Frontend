import React, { useContext, useEffect, useState } from 'react';
import api from 'axios';


//Fetch and display a lsit of videos
const VideoList = () => {
    
    //feche the list of videos from the API
    useEffect(()=>{});

    //Set a loader
    if ();  

    //Display the list of videos titls on the screen, set the click video as selected
    return ();
};

export default VideoList;