import Rect, { useState } from 'react';
import api from '../api/axios';

//Create a video submission form to add a new video
const VideoForm = () => {
    //Use state to set the video title, description and url and error

    //Handles the form submission, and makes a post request to the API
    const handleSubmit = async (e) =>{};

    // Render the form, and reset when submitted successfully
    return();
}

export default VideoForm;