import React, { useState, useContext, useEffect } from 'react';
import api from '../api/axios';
import { VideoContext } from '../context/VideoContext';
import { useParams } from 'react-router-dom';

//Display video player and video details and magae controls
const VideoPlayer = () => {
    //use params to get specific video
    //Use state to set the video and manage controls and error
    //use context to access the video list

    //Send a get request to the API
    useEffect(() => {});

    //Set loader and error if content does not load
    if();

    //Display video player and video details and magae controls
    return();
};  

export default VideoPlayer;

